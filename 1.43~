(define (repeated proc n)
	(define (iter count cumulative-proc)
		(if (= n 1)
	    	    cumulative-proc
		    (lambda (x)
		    	    (proc x)
		    )
		)
	)
	(if (> n 0)
	    (iter n cumulative-proc)
	    (error "Input should be greater than zero")
	)
)