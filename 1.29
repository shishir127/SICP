(define (cube x)
	(* x x x)
)

(define (simpsons-integral f a n b)
	(let (h (/ (- b a) n))
	     (define (sum f start n end)
		     (let (term (+ start (* h n)))
		     	  (if (>= term  end)
		     	      0
		    	      (+ (f term) (sum-even f a (+ n 2) b))
		      	  )
		     )
	      )
	      (* (/ h 3)
			(+ (f a) (f b)
			      	    (* 4 (sum cube a 1 b))
	       	          	    (* 2 (sum cube a 2 b))
	      		)
	      )
	 )
)