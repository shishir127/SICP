(define (cons x y)
  (lambda (m) (m x y)))

(define (car z)
  (z (lambda (p q) p)))

(define (cdr z)
	(z (lambda (a b) b))
)

> (define test-pair (cons "left" "right"))
> (car test-pair)
"left"
> (cdr test-pair)
"right"
