(define (sum of squares a b) (+ (* a a) (* b b)))

(define (proc a b c) (cond ((>= a b) and (>= a c))
					(if (> b c) (sum_of_squares a b)
						(sum_of_squares a c))
				((>= b a) and (b >= c))
							(if (> a c) (sum_of_squares b a))
			))
