(define (exp a b)
	(define (exp_iter count product power)
		(cond ((= count 0) product)
		      ((= count 1) (* product a))
		      (else ())
		)
	)
	(cond ((= b 0) 1)
	      ((= b 1) a)
	      (else (exp_iter (- b 1) a b))
	)
)